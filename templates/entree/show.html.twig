{% extends 'base.html.twig' %}

{% block title %}Entree{% endblock %}

{% block body %}
    <h1>Entree</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ entree.id }}</td>
            </tr> #}
            <tr>
                <th>Num_be</th>
                <td>{{ entree.numBe }}</td>
            </tr>
            <tr>
                <th>QteEntree</th>
                <td>{{ entree.qteEntree }}</td>
            </tr>
            <tr>
                <th>Produit</th>
                <td>{{ entree.produit }}</td>
            </tr>
            <tr>
                <th>Transfert</th>
                <td>{{ entree.transfert ? 'oui' : 'Non' }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ entree.prix }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entree.date ? entree.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_entree_index') }}">retour à la liste</a>
    {% if not entree.transfert %}
        <a href="{{ path('app_entree_edit', {'id': entree.id}) }}">editer</a>
    {% endif %}
    {{ include('entree/_delete_form.html.twig') }}
{% endblock %}
