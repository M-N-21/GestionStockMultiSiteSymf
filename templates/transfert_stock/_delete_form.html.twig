<form method="post" action="{{ path('app_transfert_stock_delete', {'id': transfert_stock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transfert_stock.id) }}">
    <button class="btn">Delete</button>
</form>
