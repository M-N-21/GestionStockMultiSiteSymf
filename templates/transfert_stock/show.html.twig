{% extends 'base.html.twig' %}

{% block title %}TransfertStock{% endblock %}

{% block body %}
    <h1>TransfertStock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transfert_stock.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ transfert_stock.date ? transfert_stock.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_transfert_stock_index') }}">back to list</a>

    <a href="{{ path('app_transfert_stock_edit', {'id': transfert_stock.id}) }}">edit</a>

    {{ include('transfert_stock/_delete_form.html.twig') }}
{% endblock %}
